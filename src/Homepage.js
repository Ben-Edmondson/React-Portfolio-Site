import React from "react";

const Homepage = () => {
    return(
    <div>
        <header className="App-header">
        <h1>Homepage</h1>
        <p>
          This website is built in React. It is intended as a website to showcase
            and log the work I have done while on placement
          at The Pensions Regulator
        </p>
        </header>
      </div>
    );
};

export default Homepage;